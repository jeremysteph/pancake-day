#ifndef ENUM_H
#define ENUM_H
//Max history path based on the location of the user
#define MAX_PATH    100
//100 characters is the max # of characters that can be entered by the user
#define MAX_INPUT   100

//Different Modes based on user input command
enum modes
{
    NORMAL, PIPELINE, BACKGROUND
};
#endif
